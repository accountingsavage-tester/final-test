# Audio Calculator

A single-file calculator. Enter `1+1` and press `=` to play `music.mp3` and show the lyric overlay area. Place `music.mp3` in the repository root.

The overlay is timed for 00:15 through 01:00 of the audio. Add only lyrics you have permission to use in the `#lyrics` element or provide your own text.
